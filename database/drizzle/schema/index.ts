/**
 * Schema entrypoint.
 *
 * The full V1 domain schema (schools, students, teachers, classes, grades,
 * attendance, homework, announcements, notifications, academic structure
 * concepts, etc.) is intentionally not declared yet. Schemas will be added by
 * the dedicated database schema task, after the corresponding domain rules
 * are locked in.
 *
 * Repositories live in each module's `infrastructure/` folder, never here.
 */

export {};