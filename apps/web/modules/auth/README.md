# Authentication module (placeholder)

Owned by: Authentication

Responsibilities:
- Identity (User)
- Session management
- Login / logout / recovery flows
- Account lifecycle

Authorization decisions (Role, Permission, Scope, Ownership) live in
`lib/authorization/` and inside the relevant domain modules.

This folder is a placeholder. Implementation will be added in a dedicated
authentication task.
