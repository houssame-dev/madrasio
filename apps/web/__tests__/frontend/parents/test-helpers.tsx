import { QueryClient, QueryClientProvider } from '@tanstack/react-query'; import { render, type RenderResult } from '@testing-library/react'; import type { ReactElement } from 'react'; import { AppContextProvider } from '@/components/app/app-context'; import type { Role } from '@/lib/authorization/roles';
export const schoolId = '00000000-0000-4000-8000-000000000201'; export const parentId = '00000000-0000-4000-8000-000000000202'; export const userId = '00000000-0000-4000-8000-000000000203'; export const studentId = '00000000-0000-4000-8000-000000000204'; export const otherStudentId = '00000000-0000-4000-8000-000000000205'; export const relationshipId = '00000000-0000-4000-8000-000000000206'; export const timestamps = { createdAt: '2026-01-01T00:00:00.000Z', updatedAt: '2026-01-01T00:00:00.000Z' };
export const parent = { id: parentId, firstName: 'Samira', lastName: 'Alaoui', parentCode: 'P-01', userId, status: 'ACTIVE' as const, ...timestamps }; export const student = { id: studentId, firstName: 'Nora', lastName: 'Alaoui', studentCode: 'S-01', status: 'ACTIVE' as const, ...timestamps }; export const relationship = { id: relationshipId, parentId, studentId, status: 'ACTIVE' as const, ...timestamps };
export function page<T>(data: T[], pageNumber = 1, pageSize = 20, total = data.length) { return { data, meta: { page: pageNumber, pageSize, total } }; }
export function renderParents(ui: ReactElement, role: Role = 'SCHOOL_ADMIN'): RenderResult & { queryClient: QueryClient } { const queryClient = new QueryClient({ defaultOptions: { queries: { retry: false }, mutations: { retry: false } } }); const result = render(<QueryClientProvider client={queryClient}><AppContextProvider value={{ user: { id: userId }, currentSchool: { id: schoolId, role }, memberships: [{ schoolId, schoolName: 'Atlas School', role, status: 'ACTIVE' }] }}>{ui}</AppContextProvider></QueryClientProvider>); return Object.assign(result, { queryClient }); }
