# `lib/files`

File storage boundary (Cloudflare R2).

Implements file upload, signed access, and storage key generation. Authorization
helpers ensure source-resource authorization is enforced before any signed URL
is generated.
