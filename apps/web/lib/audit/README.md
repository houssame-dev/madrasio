# `lib/audit`

Audit event persistence boundary.

Audit records are cross-cutting accountability records (not generic logs).
Concrete audit event types will be defined per module when they need them.
