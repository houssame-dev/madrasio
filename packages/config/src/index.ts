/**
 * Shared config package.
 *
 * Intentionally minimal in V1. Provides tsconfig and tailwind preset
 * re-exports so apps and packages can stay aligned without duplicating
 * configuration. No business logic lives here.
 */

export {};
