# `scripts/`

Workspace-level operational scripts.

Currently empty — this directory will host maintenance scripts (e.g. seed
data, schema verification) added in dedicated later tasks.

Bootstrap does not introduce any business logic here.
